import { PaysDirective } from './pays.directive';

describe('PaysDirective', () => {
  it('should create an instance', () => {
    const directive = new PaysDirective();
    expect(directive).toBeTruthy();
  });
});
